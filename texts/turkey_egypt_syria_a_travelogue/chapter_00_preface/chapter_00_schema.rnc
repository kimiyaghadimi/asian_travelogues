datatypes xsd = "http://www.w3.org/2001/XMLSchema-datatypes"

start = body

body = element body {div*}
    div = element div {type, n, chapter_title, head, p, trailer}
        type = attribute type { "chapter"}
        n = attribute type {"1"} # number count for chapter etc?
        head = element head {lg}
            lg = element lg {l}
                l = element l {text}
        p = element p { mixed { ( persName | placeName | orgName | roleName | state | seg | date | time | lg | l | language | q | said | event | rs) } }
                persName = element {text}
                placeName = 
                orgName = 
                roleName = 
                state = 
                seg = 
                date = 
                time = 
                lg = 
                l = 
                language = 
                q = 
                said = 
                event = element {text}



# empty tags about the page number and corresponding subject?
# stuff for footnotes etc

